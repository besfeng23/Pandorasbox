# Runtime environment
APP_ENV=dev

# Cloud Run Secrets URL (preferred runtime config source)
# Fetches: ${CLOUDRUN_SECRETS_BASE_URL}/pandorasbox/${APP_ENV}.json
CLOUDRUN_SECRETS_BASE_URL=
# Optional bearer for secrets URL (server-side only)
CLOUDRUN_SECRETS_BEARER=

# Fallback env vars (examples â€” set real values in your environment/secrets provider)
OPENAI_API_KEY=
MCP_API_KEY=
KAIROS_BASE_URL=
KAIROS_EVENT_GATEWAY_URL=